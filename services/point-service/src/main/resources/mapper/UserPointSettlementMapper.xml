<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mingri.train12306.biz.pointservice.dao.mapper.UserPointSettlementMapper">

    <!-- 根据消费方事务ID查询对账记录 -->
    <select id="selectByConsumerTransactionId" resultType="com.mingri.train12306.biz.pointservice.dao.entity.UserPointSettlementDO">
        SELECT *
        FROM t_user_point_settlement
        WHERE user_id = #{userId}
          AND consumer_transaction_id = #{consumerTransactionId}
    </select>

    <!-- 根据提供方业务ID查询对账记录 -->
    <select id="selectByProviderBusinessId" resultType="com.mingri.train12306.biz.pointservice.dao.entity.UserPointSettlementDO">
        SELECT *
        FROM t_user_point_settlement
        WHERE user_id = #{userId}
          AND provider_business_id = #{providerBusinessId}
    </select>

    <!-- 根据消费方业务ID查询对账记录 -->
    <select id="selectByConsumerBusinessId" resultType="com.mingri.train12306.biz.pointservice.dao.entity.UserPointSettlementDO">
        SELECT *
        FROM t_user_point_settlement
        WHERE user_id = #{userId}
          AND consumer_business_id = #{consumerBusinessId}
    </select>

    <!-- 批量插入对账记录 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_user_point_settlement (
            user_id,
            consumer_source,
            consumer_code,
            provider_source,
            provider_code,
            consumer_transaction_id,
            consume_point,
            provider_business_id,
            consumer_business_id,
            create_time,
            update_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.userId},
                #{item.consumerSource},
                #{item.consumerCode},
                #{item.providerSource},
                #{item.providerCode},
                #{item.consumerTransactionId},
                #{item.consumePoint},
                #{item.providerBusinessId},
                #{item.consumerBusinessId},
                #{item.createTime},
                #{item.updateTime}
            )
        </foreach>
    </insert>

</mapper>
